const birthday = '18.04.1982';
const age = someCode(birthday);

const BIRTHDAY = '18.04.1982'; // make uppercase? - This never changes; so yes.
const AGE = someCode(BIRTHDAY); // make uppercase? - This does change, so no.
