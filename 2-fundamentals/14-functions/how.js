// The name of the function goes after the function
// Then the number of parameters goes in brackets `()` means none
// Then in between the bracers is the method signature or body

function showMessage() {
  console.log('Hello everyone!');
}

showMessage();
showMessage();